const {gql} = require('apollo-server');

const commonSchema = gql`
    scalar DateTime

    type Error{
        code: String
        message: String 
        token: String
    }
`
module.exports = commonSchema
